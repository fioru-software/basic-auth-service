generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- name: basic-auth-config
  files:
  - default.conf

secretGenerator:
- name: basic-auth-secret
  files: 
    - .htpasswd

resources:
  - deployment.yaml
  - service.yaml

